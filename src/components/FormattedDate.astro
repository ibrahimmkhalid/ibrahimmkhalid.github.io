---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---
<time datetime={date.toISOString()}>{
		date.toISOString('en-us', {
			year: 'numeric',
			month: 'numeric',
			day: 'numeric',
      }).substring(0,10)
}</time>
